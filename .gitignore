node_modules/
data/
dist/
build/
build-test/
dist-dxt/
out/
.DS_Store
*.log
.idea/
.claude

# Electron build output
src/electron/dist/
release/
package-manifest.txt
.env

# Downloaded by frontend/scripts/fetch-model.mjs for the web build's
# self-hosted embedding model — regenerated on demand, not source.
frontend/public/assets/models/

# graphify — the queryable graph (graph.json, GRAPH_REPORT.md, manifest.json)
# stays tracked so a fresh clone can run `graphify query` without rebuilding.
# These are regenerated on every `graphify update` and are pure churn:
graphify-out/cache/
graphify-out/graph.html
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
